/*
 * Given a input string return a object in the form
 *    {
 *        identified: boolean,
 *        type: string
 *    }
 * which identifies if the string is a phone number,
 * email, geo coordinates or twitter handle.
 * if the input string matches one of these set identified
 * to true and type to what the match is i.e "phone number"
 * if no match can be found set identified to false
 * and type to undefined.
 */

function identify (string) {
  
}

module.exports = identify;
