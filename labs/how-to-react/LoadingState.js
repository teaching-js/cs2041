import StatefulClass from './State';
// Excercise 2:
// Create a class called 'LoadingContainerClass' which
// - Upon construction, takes in a loadedHandler, a promise and an Error Handler function
// - The state should contain 'loading, loaded, data and error, errorMsg' as it's defined states, which all by default should be set to false.
// - Upon construction,
class LoadingContainerClass extends StatefulClass {

}

module.exports = LoadingContainerClass;
