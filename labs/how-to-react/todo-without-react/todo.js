(function() {
  // create a handler function for when form is submitted
    // create an element of id that's 'todo-list-item-x' under the 'todo-list' element.
    // Create the remove todo-item handler and attach it to the element of id "remove-todo-item-x" 
  // bind the handler function with the input box or form submit
})();
